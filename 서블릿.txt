<!-- 서블릿 생성 > 컨트롤러 역할 -->
  <servlet> 
  	<servlet-name>login</servlet-name>                                    <!-- 서블릿 매핑할 이름 -->
  	<servlet-class>org.project.controller.MemberLogin</servlet-class>	  <!-- 실제 서블릿 클래스 위치 -->
  </servlet>
  
  <!-- 서블릿 매핑 -->
  <servlet-mapping>
  	<servlet-name>login</servlet-name>      <!-- 서블릿 매핑할 이름 -->
  	<url-pattern>/login.do</url-pattern>    <!-- 클라이언트 요청 url(조건)=> 해당 url로 요청을 받으면 해당 클래스를 실행 -->
  </servlet-mapping>
  
  
  <!-- join 서블릿 생성 -->
 <servlet>
  	<servlet-name>join</servlet-name>
  	<servlet-class>org.project.controller.MemberInsert</servlet-class>
  </servlet>
  
  <servlet-mapping>
  	<servlet-name>join</servlet-name>
  	<url-pattern>/insert.do</url-pattern>
  </servlet-mapping>
  
  
  <!-- update 서블릿 생성 -->
  <servlet>
  	<servlet-name>update</servlet-name>
  	<servlet-class>org.project.controller.MemberUpdate</servlet-class>
  </servlet>
  
  <servlet-mapping>
  	<servlet-name>update</servlet-name>
  	<url-pattern>/update.do</url-pattern>
  </servlet-mapping>